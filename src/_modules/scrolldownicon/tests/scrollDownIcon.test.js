'use strict';

import ScrollDownIcon from '../scrolldownicon';

describe('ScrollDownIcon View', function() {

  beforeEach(() => {
    this.scrollDownIcon = new ScrollDownIcon();
  });

  it('Should run a few assertions', () => {
    expect(this.scrollDownIcon).toBeDefined();
  });

});
